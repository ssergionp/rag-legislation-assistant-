package com.sergio.legisassistant.service;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class IngestionServiceUnitTest {

    /**
     * Placeholder test verifying the truncation-adjacent contract used by
     * ChatService. A full integration test (real vector store + real
     * embedding model) needs Testcontainers with the pgvector image and is
     * listed as a follow-up in CLAUDE.md.
     */
    @Test
    void placeholderUntilIntegrationTestsAreAdded() {
        assertThat(true).isTrue();
    }
}
